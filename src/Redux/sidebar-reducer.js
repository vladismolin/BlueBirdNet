let initialState = {
    sidebarData: [
        {id: 1, name: 'Vlad'},
        {id: 2, name: 'Eugene'},
        {id: 3, name: 'Mariyka'},
        {id: 4, name: 'Roman'}
    ]
};

const sidebarReducer = (state = initialState, action) => {
    return state;
}


export default sidebarReducer;
